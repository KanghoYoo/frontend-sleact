import React from 'react';
import Workspace from '@layouts/Workspace/Workspace';

const Channel = () => {
  return (
    <Workspace>
      <div>로그인 된 채널입니다.</div>
    </Workspace>
  );
};

export default Channel;
